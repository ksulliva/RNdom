for_lane :nerdery_qa do
	# UPDATE THESE VALUES FOR YOUR PROJECT
	app_identifier 			"com.ksulliva.petfinder-rn" # The bundle identifier of your app
	ENV["apple_id"]			= "ksulliv6@gmail.com"
	ENV["team_id"]			= "45688JGH49"
	ENV["app_name"]			= "RNdom" # Used for the Apple Developer Account App ID registration
	ENV["scheme"] 			= "RNdom" # The xcode scheme to build
	ENV["configuration"] 	= "Release" # The project configuration to build, e.g. Release
	ENV["info_plist_path"] 	= "RNdom/Info.plist" # Path to the project's info.plist
	ENV["project_path"] 	= "RNdom.xcodeproj" # Path to the xcodeproj
	ENV["target_name"]		= "RNdom" # The name of your xcode project's target
	#ENV["xcode_version"]	= "~> 9.4" # The version of XCode to use. Leave commented out to use latest version.
end

# you can even provide different app identifiers, Apple IDs and team names per lane:
# More information: https://github.com/fastlane/fastlane/blob/master/fastlane/docs/Appfile.md